spring:
  application:
    name: licensing-service
  cloud:
    config:
      uri: http://localhost:8071

---
spring:
  config:
    activate:
      on-profile: dev
  cloud:
    config:
      uri: http://config-server:8071